<!DOCTYPE html>
<html>
<head>
</head>
<body>  
<?php
use PHPMailer\PHPMailer\PHPMailer;
use PHPMailer\PHPMailer\Exception;
require 'assets/php/Exception.php';
require 'assets/php/PHPMailer.php';
require 'assets/php/SMTP.php';

//Create an instance; passing `true` enables exceptions
//$mail = new PHPMailer(true);

//try {
    //Ajuste de servidor 
  //  $mail->SMTPDebug = 2;  // pasar a 0 cuando este en la pagina oficial                     //Enable verbose debug output
  //  $mail->isSMTP();                                       
   // $mail->Host       = 'smtp.example.com';   // colocar el SMTP de akky  (usuario) servidor de  envio de correos o servidor de correo        
   // $mail->SMTPAuth   = true;                                   //
  //  $mail->Username   = 'user@example.com';                     //Usuario de correo(Cuenta dedicada a pagina WEB )
   // $mail->Password   = 'secret';                               //Coloca la contraseña de correo 
   // $mail->SMTPSecure = ssl ;                                   //VErificar que este activado en pcb          
   // $mail->Port       = 465;     //aqui va el puerto                                //TCP port to connect to; use 587 if you have set `SMTPSecure = PHPMailer::ENCRYPTION_STARTTLS`

    //Destinatarios 
  //  $mail->setFrom('from@example.com', 'Pagina PCB de Mexico');  //Quien envia Correo 
  //  $mail->addAddress('compras@pcbdemexico.com.mx', 'Departamento de Compras');     //Quien lo recibe
  //  $mail->addAddress('ventas@pcbdemexico.com.mx' ,'Departamento de Ventas');       //Otro destinatario
  //  $mail->addAddress('ingenieria@pcbdemexico.com.mx' 'Departamento de Ingenieria')  
 //   $mail->addReplyTo('jemjia404@gmail.com', 'Jesus (prueba)');


    //Archivos 
  //  $mail->addAttachment('/var/tmp/file.tar.gz');         //Add attachments
   // $mail->addAttachment('/tmp/image.jpg', 'new.jpg');    //Optional name

    //Contenido 
 ////   $mail->isHTML(true);                                  //Set email format to HTML
 //   $mail->Subject = 'Prueba de envio de correos desde Pagina WEB';
 //   $mail->Body    = 'Cuerpo de correo con atributos HTML con utf 8 <h1>verificacion de encriptado ssl </h1> <b>in bold!</b>';
  //  $mail->AltBody = 'This is the body in plain text for non-HTML mail clients';
//
//    $mail->send();
 //   echo 'El correo de envio correctamente';
//} catch (Exception $e) {
//    echo "Error de envio codigo de referecia: {$mail->ErrorInfo}";
//}

//varaiables de formulario PCB 
$ArchivoPCB=$NombrePCB=$EmailPCB=$EmpresaPCB=$TelefonoPCB=$CantidadPCB=$MaterialPCB=$ColorMAPCB=$SerigrafiaPCB="";
$NCarasPCB=$EspesorPCB=$Onzas=$PTHPCB=$DXPCB=$DYPCB=$UnidadesPCB=$AcabadosPCB=$CantidadDDPCB=$CantidadDPPCB=$ExtrasPCB="";
//if(isset())
$ArchivoPCB=$_POST('Archivo_PCB');
$NombrePCB=$_POST('nombrePCB');
$EmailPCB=$_POST('EmailUsuPCB');
$EmpresaPCB=$_POST('EmpresaPCB');
$TelefonoPCB=$_POST('TELPCB');
$CantidadPCB=$_POST('CantidadPCB');
$MaterialPCB=$_POST('rdMaterial');
$ColorMAPCB=$_POST('rdColor');
$SerigrafiaPCB=$_POST('rdSER');
$NCarasPCB=$_POST('rdCaras');
$EspesorPCB=$_POST('rdG');
$Onzas=$_POST('rdOz');
$PTHPCB=$_POST('PTHS');
$DXPCB=$_POST('largoP');
$DYPCB=$_POST('AltoP');
$UnidadesPCB=$_POST('Unidad');
$AcabadosPCB=$_POST('sellist1');
$CantidadDDPCB=$_POST('BrocasA');
$CantidadDPPCB=$_POST('Perforaciones');
$ExtrasPCB=$_POST('SecExtras');
 echo $ArchivoPCB;
 echo $NombrePCB;
 echo $EmailPCB;

// Variables de formulario Impresion
$ArchivoIMP=$NombreIMP=$EmailIMP=$TelefonoIMP=$CantidadIMP=$MaterialIMP=$ExtrasIMP=$EscaladoIMP="";



?>

</body>
</html>